
var n = 0;

/*
while (true)
{
    window.setInterval(function(){
        if(leftPosition > 0 && n == 5) {
            console.log("entro 2")
            track.style.left = `${-1 * (leftPosition - slickWidth)}px`;
        }
        if(leftPosition < (trackWidth - listWidth) && n == 10) {
            track.style.left = `${-1 * (leftPosition + slickWidth)}px`;
        }
      console.log(n);
      n++;
    },1000);
}

*/